{
  "mcpServers": {
    "plugin_doctor": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server.js"],
      "env": {
        "PLUGIN_DOCTOR_ROOT": "${CLAUDE_PROJECT_DIR}"
      },
      "alwaysLoad": true
    }
  }
}
